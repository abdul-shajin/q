<% provide(:title, 'Home') %>
<div class="span8">
    <% if @quotes.any? %>
      <ol class="quotes">
        <%= render @quotes %>
      </ol>
      <%= will_paginate @quotes %>
    <% end %>
</div>
